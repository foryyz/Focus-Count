# FocusCount

专注计时与时间标记工具。提供 macOS 与 iPhone 原生 SwiftUI 客户端；Windows 11 客户端尚未实现。iPhone 开发与运行方式见 [apps/ios/README.md](apps/ios/README.md)。

## 当前版本

- **macOS 1.12.1**：投入趋势改为单色曲线、浅色面积填充与可选 7 日平均；保留独立分析窗口、活动分类与自定义颜色；支持可选计时状态接续；导入合并完成后显示“新增 X 个”，分别列出专注记录与时间标记数量（含最近删除）。按合并成功后的实际新增 ID 统计，重复导入不会重复计数。
- **iPhone 1.2.0**：新增与 Mac 双向接续计时；支持频率总览、折线趋势、24 小时时间分布，以及颜色、emoji 设置和标记编辑；支持横屏全屏查看。
- 两端使用 JSON v5 手动导出、导入并合并数据；颜色和 emoji 设置仍各自保存在本机。

## 安装与更新

### Mac

当前本地打包产物为 [FocusCount-1.12.1-macOS.zip](dist/macos/FocusCount-1.12.1-macOS.zip)（约 2.5 MB），适用于 **Apple 芯片 Mac、macOS 13 及以上**。

1. 退出正在运行的 FocusCount。
2. 解压 ZIP，将 `FocusCount.app` 放入“应用程序”文件夹；更新时替换旧应用。
3. 打开新版应用，原有数据仍保存在当前用户的应用数据目录。

压缩包只包含应用，不包含个人记录。接收方无需项目源码或 Xcode。该应用采用本地 ad-hoc 签名，未进行 Apple 公证；系统可能要求确认打开。当前压缩包不适用于 Intel Mac。

`dist/` 不进入 Git，以上链接指向本地构建产物；新克隆项目请按下文构建，或向分发者获取压缩包。

### iPhone

打开 [FocusCount.xcodeproj](apps/ios/FocusCount.xcodeproj)，连接 iPhone，选择原 Apple Team 和设备，按 **Command-R** 编译并覆盖安装。更新时保持相同 Bundle Identifier，不要先卸载旧应用，以保留数据。Mac ZIP 和 iOS 模拟器产物不能安装到 iPhone；详细步骤见 [iPhone 说明](apps/ios/README.md)。

## 项目结构

```text
FocusCount/
├── .focuscount-root          旧项目数据迁移定位标记，独立运行不需要
├── apps/
│   ├── macos/                Swift Package、Sources、Tests
│   ├── ios/                  iPhone Xcode 工程、源码与测试
│   └── windows/              后续 Windows 开发说明
├── packages/FocusCountCore/  Mac / iPhone 共用模型与数据逻辑
├── data/                     旧项目数据，迁移后保留，个人记录不进入 Git
├── docs/data-format.md       两个平台共同遵守的数据协议
├── scripts/build-app.sh      macOS 构建与本地签名
└── dist/macos/               Mac 应用与 ZIP 压缩包，不进入 Git
```

后续 Windows 客户端放入 `apps/windows/`，构建产物放入 `dist/windows/`，计划遵循相同的 JSON 数据协议；Windows 本地存储位置尚未实现。

## macOS 构建与运行

要求 macOS 13+、Swift 5.9+。从项目根目录运行：

```sh
bash scripts/build-app.sh
open dist/macos/FocusCount.app
```

脚本生成本机架构的应用并进行 ad-hoc 签名，未进行 Apple 公证。也可开发运行：`swift run --package-path apps/macos`。

编译并打包当前版本（在项目根目录运行）：

```sh
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer bash scripts/build-app.sh
ditto -c -k --sequesterRsrc --keepParent dist/macos/FocusCount.app dist/macos/FocusCount-1.12.1-macOS.zip
unzip -t dist/macos/FocusCount-1.12.1-macOS.zip
```

**应用可以独立运行**，可将 `dist/macos/FocusCount.app` 复制到“应用程序”目录，也可压缩后发给其他兼容 Mac。接收方无需项目源码、根目录标记或 Xcode。当前脚本生成本机架构，Apple 芯片构建不支持 Intel Mac；要求 macOS 13+。

数据独立保存在当前用户的 `~/Library/Application Support/FocusCount/`，不会打包进应用。升级现有项目时，先从原项目目录启动一次新版，完成自动迁移，再移动应用。若先移动了应用，可手动导入原项目的 `data/sessions.json`。

测试（需要完整 Xcode）：

```sh
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swift test --package-path apps/macos
```

修改源码后，先退出旧应用，再重新构建、启动。

本次目录调整保留了原应用于 `dist/legacy/FocusCount.app`（仅作备份，仍使用旧数据路径）；原 `dist/FocusCount.app` 入口已改为指向新版应用的符号链接。日常请使用 `dist/macos/FocusCount.app`。

## 使用

- 点击大号时间，或在空命令输入框中回车：开始、暂停、继续。
- 点击带停止图标的“结束”：填写活动与专注度 S–D 后保存。
- “返回计时”保留进度并恢复为暂停状态。
- 暂停不累计时间，系统熄屏和休眠继续累计，关闭窗口退出。
- 运行中约每 5 秒保存草稿，操作时立即保存，重启后恢复为暂停状态。
- 主界面默认 920×560，可自由缩放（开始页最小 720×440，计时页最小 720×520），支持原生全屏及右上角全屏按钮。暖白／深石墨底色，时分突出、秒稍小，颜色随系统外观变化。
- 默认隐藏历史列表与统计，点击右上角“专注记录”图标打开管理页；左上角“返回计时”或 Esc 可返回并恢复键盘操作。
- 记录页支持日期、科目、专注度筛选；编辑、补记、删除和恢复。
- 记录页右上角“专注分析”打开独立分析窗口；图表与活动分类见下方说明。
- 编辑保留原记录 ID；起止时间与有效时长分别填写，暂停不计时。
- “最近删除”不计入统计，可随时恢复，不自动清空。
- 完成学习与编辑时可选择已有科目；支持深色模式。

## 数据迁移与同步

Mac 的主数据保存于 `~/Library/Application Support/FocusCount/sessions.json`，CSV 自动生成，备份在同目录 `backups/`。数据管理页面提供“打开数据目录”和“打开备份目录”。

首次从带有 `.focuscount-root` 的旧项目运行新版时，自动发现 `data/sessions.json`，验证并备份双方原始文件，再按共享合并规则迁移。如果本地已有数据，保留本地计时草稿；项目草稿保存在迁移备份中。原项目文件及其旧备份不会删除。迁移完成后创建 `project-storage-migration.json` 标记，不会反复导入项目旧数据。迁移失败时停止写入并显示错误。

不同设备通过应用中的 JSON 导出和导入合并交换数据；不要直接覆盖运行中的主文件。移动或更新应用不影响用户数据，发送应用也不会发送个人记录。

数据协议见 [docs/data-format.md](docs/data-format.md)。当前写入版本为 v5，包含可恢复的历史版本；首次读取 v1/v2/v3 数据会保留对应版本备份，旧应用不能继续读写新版数据。为兼容原记录，JSON 日期仍使用自 2001 年起的秒数；CSV 日期为 ISO 8601 UTC。

## 版本控制

Git 跟踪源码、脚本、文档和根目录标记；忽略个人学习数据、编译缓存和 `dist/` 产物。新克隆的项目需要重新构建应用；已有记录请通过 JSON 导入合并或从备份恢复，旧项目的 `data/` 仅用于迁移。

## 跨设备接续计时（Mac 1.11.0 / iPhone 1.2.0）

1. 在原设备的数据管理中导出 JSON，传到另一台设备。
2. 在接收设备导入文件，预览中可查看导出时间、计时状态、累计时长与活动名称。
3. 勾选“同步计时状态”，再确认合并。未勾选时仅合并记录；该选项每次导入默认关闭。

运行中会补上导出到导入之间的时间并继续计时；暂停保持暂停；待保存保持待保存，关闭数据管理后可完成保存。未开始状态也可同步，会清空接收设备当前计时。接收设备的原状态会在替换前备份。

这是手动接续，不是实时联网同步：原设备不会自动停止，建议只在接续设备继续操作。两端分别修改计时时，以本次明确选择导入的文件为准，不相加时长；旧文件可能恢复较早的暂停状态，请核对导出时间。同一次接续计时保留相同 ID，保存后合并不会重复新增；已保存或彻底删除的计时不能再通过旧运行快照接续。

两端需更新到上述版本后重新导出。旧文件仍可合并记录，但无法同步运行状态。Mac 重启仍恢复为暂停，iPhone 保留其运行时间基准；跨设备计时依赖系统时间，请保持设备时钟准确。

## Mac / iPhone 双向合并

Mac 与 iPhone 首页右上角的“数据管理”都支持 JSON 导入、预览、确认合并、导出与历史版本恢复。相同 ID 自动去重，较新修改作为当前版本，其他版本归档保留，不重复计入统计；同时间冲突按固定规则选定，两端结果一致。导入前备份双方数据，默认保留本机计时；勾选“同步计时状态”后接续文件中的计时。

Mac 合并成功后提示“合并完成，新增 X 个（专注记录 X 个，时间标记 X 个，含最近删除）”。这个数量只统计本机此前没有、且本次实际保留下来的记录；更新已有记录、增加历史版本不算新增，被彻底删除规则过滤的记录也不计入。重复导入相同文件会显示“新增 0 个”。

两端都更新后再交换 v5 文件。请选择“导入并合并”，不要直接覆盖正在运行的应用数据文件。此功能不提供后台云同步；完整规则见数据协议。

“最近删除”支持单条彻底删除与清空全部（包含筛选外记录），确认后移除记录及其历史版本，仅保留防止旧文件导入复活的 ID。已有独立备份不受影响。

Mac 开始计时后可点击底部叉号按钮取消本次计时，确认后归零，不生成学习记录；休眠前若手动暂停，唤醒后仍保持暂停。退出应用后重新打开仍按已保存草稿恢复为暂停。

## Mac 指令与时间标记

- 点击结束按钮保存专注；`!stop` 现在只标记 stop，不再结束计时。
- `!文字`：记录标记及当下时间，英文统一小写，如 `!sex` → sex、`!Sad` → sad；支持中文和词组，不改变当前计时。
- 开始页可直接在活动输入栏输入 `!文字` 并回车添加标记，不启动计时。计时页点击“标记”展开输入框；成功标记后自动收起，Esc 可收起。空标记不保存。未编辑标记时可通过回车开始、暂停或继续。

时间标记位于“专注记录 → 时间标记”，支持删除、恢复及彻底删除，不参与专注时长、次数或图表统计；随 JSON 自动去重合并。CSV 仍只导出时长记录，完整备份请使用 JSON。iPhone 的记录页提供时间标记管理，支持相同的 `!文字` 语法，点击“结束”保存专注记录。两端应更新后交换 v5 数据。

## Mac 1.5 沉浸专注

准备页面采用英文鼓励语与 emoji，可选填写活动名称或选择历史活动，开始后显示在时间上方；未填写时不显示占位文案。完成保存后给出短暂鼓励并切换下一句文案。

专注中持续播放低速波纹，暂停时静止；开启系统“减少动态效果”后不播放循环动画。全屏运行时，鼠标静止约 4 秒后隐藏操作区，移动鼠标或输入指令即可恢复；暂停与错误提示时操作区保持可见。全屏不会自动改变系统勿扰设置。休眠计时和数据交换功能保持可用；最新标记语法见上文。

Mac 1.5.1：暂停时显示“已暂停 · PAUSED”胶囊提示，数字采用暖琥珀色与轻字重，波纹位置变为“继续专注”按钮；运行时恢复高对比数字和循环波纹。字号与布局保持稳定，避免切换时跳动。

Mac 1.6.1：精简开始页，移除顶部标语与底部操作栏；专注记录、数据管理和全屏图标统一排列于右上角，全屏位于最右。计时页操作依次为“暂停／继续、标记、取消、结束”，取消仍保留确认。数据格式仍为 v5，新自定义标记可被 iPhone 显示和合并。

Mac 1.6.2：开始按钮采用彩虹渐变、细腻流光和悬停光晕，支持系统“减少动态效果”。移除按钮下方提示，将对应英文文案加入随机鼓励语；每次打开应用随机选择一句，完成或取消后随机切换且不连续重复。

Mac 1.6.3：暂停页移除中央继续按钮，恢复琥珀色静止波纹；底部继续、点击时间和回车仍可恢复计时。开始页加大主文案、辅助文字和按钮字号，重新调整分组间距，默认窗口为 920×560；系统已记住的用户窗口尺寸仍会保留。

## Mac 时间标记（1.11.0）

入口：右上角“专注记录” → “时间标记”。页面为可视化、时间轴、最近删除。iPhone 1.1.0 已同步核心可视化能力，并适配手机交互。

时间标记使用独立的 Mac 窗口，可拖动标题栏移动，支持最小化、全屏和拖动边缘调整大小，并记住窗口位置与尺寸。默认内容区为 1440 × 810（16:9）；升级后首次使用新的默认尺寸，此后保存用户调整。最小内容尺寸为 1000 × 700；图表与日期导航随可用空间扩展，侧栏宽度保持不变。“返回记录”关闭此窗口，原记录页面保留。

- 可视化默认进入「频率总览」：每种标记独占一行，气泡表示对应日期内的次数；空白日期保留。所有行共享面积比例，极端值达到尺寸上限后显示数字，悬停或点击均可查看精确次数。空间足够时气泡显示设置的 emoji，密集视图使用颜色，行首始终保留标记名称和 emoji。
- 本周默认完整展示周一至周日，未来日期明确标注；30 天完整展示每天；全部从最早未删除标记至今天。自动粒度在 45 天以内按天、280 天以内按周，更长按月；也可以手动选天、周、月。周以周一开始。未完整的周、月带底线提示，详情显示覆盖天数。
- 日期栏可拖选范围放大，支持缩放按钮和触控板捏合；底部全周期频率概览支持拖动选框浏览、拖动两端缩放。「查看全貌」恢复完整周期。缩放不改变页面的总次数与平均频率统计范围。手动选择很密的粒度时可以横向滚动，标记名称固定在左侧。
- 点击气泡在右侧查看全部原始记录和小时分布；周、月可进一步展开每日分布。可以修改单条标记的内容和时间，或移至最近删除。编辑保留记录 ID 并更新时间戳，旧文件再次导入不会覆盖较新的编辑。
- 「折线趋势」按每日次数绘制，无记录日期计为零；「时间分布」独立展示 24 小时点图，全天一屏，最小缩放为 1.0×，可展示完整日期范围；底部选框也可拉伸至整个周期。拥挤时仅合并同一天相邻时段的同种标记；合并 emoji 使用同色描边和浅底，尺寸随次数增加，最大为 72 点（单条为 24 点）。点击可查看原始时间。
- 平均频率为 `范围内次数 ÷ 范围天数 × 7`：本周按周一至今天（包含今天），30 天按 30 天，全部按最早标记至今天，包含无记录日期。不同标记使用相同统计范围。
- 点击左侧标记图标设置颜色和 emoji（可留空）。设置保存在当前 Mac 的应用偏好中，暂不随 JSON 交换。时间轴与最近删除的原有功能保留，数据格式不变。

## iPhone 时间标记（1.1.0）

- 记录页进入“时间标记”，可切换可视化、时间轴和最近删除；周期为本周、30 天、全部。
- 频率总览、折线趋势、时间分布与 Mac 共用统计和图表组件，平均每周次数采用相同计算规则。
- 时间分布最小缩放为 1.0×，拥挤的同类 emoji 合并后放大并加彩色描边；点击可查看原始时间。
- 点击“全屏”并旋转手机，可使用横屏紧凑布局查看完整 24 小时时间分布；进入、退出全屏保留当前图表模式。
- 选择标记后可设置颜色与 emoji；气泡详情通过底部面板显示，支持进入编辑或删除。编辑保留 ID，仍兼容两端 JSON 合并。

验证：Mac 50 项、iPhone 27 项、共享逻辑 12 项测试通过，覆盖计时运行／暂停／待保存的跨端转换、重复导入、旧文件兼容、完成后的去重和过期运行快照保护。Mac ZIP 已校验；iPhone 真机安装需通过 Xcode 完成。

## Mac 专注分析（1.12.1）

入口：专注记录 → 右上角「专注分析」。原记录页保留明细、筛选、补记和编辑，图表移至独立窗口。分析窗口支持拖动、缩放、全屏，记住尺寸；默认约 1280 × 720，并适配可用屏幕空间。

- 同屏展示每日投入趋势与时间去向。左侧为总时长单色曲线和浅色面积，筛选分类或活动后使用对应颜色；右侧仍可按活动或分类汇总。分类示例：学习、工作、娱乐、运动。
- 周期支持本周、30 天、90 天、全部与自定义；可按分类和活动筛选。本周显示完整周一至周日，未来日期留空；日均仅计算周一至今天。
- 顶部统计有效时长、日均时长、专注次数与有专注的天数。日均包含筛选周期内的空白日期，最近删除不参与统计。
- 趋势按日期保留空白位置，90 天以内按天、730 天以内按周、更长按月汇总。点击日期展开原始记录，可直接编辑；周、月详情可放大为更细粒度，点击“查看全貌”恢复完整周期。缩放图表不改变顶部和右侧的整体筛选周期。
- 「分类与颜色」中创建、重命名或删除分类，为活动分配分类，并调整活动和分类颜色。每个活动只属于一个分类，未分配的归入“未分类”；删除分类不删除记录。分类变更应用于同名活动的全部历史记录。
- 分类与颜色存入当前 Mac 应用偏好 `focus-analysis-appearance-v1`，暂不随 JSON 交换，iPhone 本轮未增加分类界面。活动改名后视为新活动，需重新归类。
- 时长按记录开始日期归属，跨午夜不拆分；未记录暂停区间，因此不推测一天中的精确专注时段。

验证包含 50 项 Mac 测试：分类持久化、颜色稳定、无记录日期、跨午夜、夏令时、长周期汇总及既有功能回归；使用独立示例数据检查图表、日期明细和分类管理。

### 投入曲线（1.12.1）

- 本周逐日标注 `2h 35m` 等时长，今天用较大的实心圆点强调；无记录日落在零线上，未来日期不连线。
- 30 天、90 天等按天展示的长周期可开启「7 日平均」虚线，默认关闭。平均按当天及此前 6 个自然日计算，包含无记录日期，也包含当前筛选周期开始前的日期；遵循当前分类和活动筛选。
- 悬停在图表上方显示所选日期、有效时长和次数，点击继续打开记录明细。长周期自动按周／月汇总时明确标注单位，不显示 7 日平均；放大到按天后可再次使用。
- 曲线使用直线连接，避免平滑插值产生不真实的峰值；右侧时间去向保持原有布局和交互。
