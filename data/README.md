# 共享数据目录

Mac 和未来的 Windows 客户端均使用此目录。个人记录不会提交到 Git。

- `sessions.json`：唯一主数据，包含历史记录、当前草稿与待完成状态。
- `sessions.csv`：从 JSON 生成的表格，不用于恢复或双向同步。

同步前退出应用，等待同步完成后再在另一端打开。请勿同时运行两个客户端写入这份数据；当前没有多端合并或跨进程锁。同步工具必须包含 `sessions.json`，发生冲突时先保留双方副本，不要直接覆盖。

数据格式见 `../docs/data-format.md`。
